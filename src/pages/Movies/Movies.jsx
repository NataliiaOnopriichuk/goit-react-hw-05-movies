// import PropTypes from 'prop-types'

export const Movies = ({ title, backdrop_path, id }) => {
  return (
    <div>
      <form>
        <input type="text" />
        input
      </form>
    </div>
  );
};

// Layout.propTypes = {}
